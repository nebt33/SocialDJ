To compile: install a C compiler, make, and QT5, then run `make` in this directory.

The server will accept IPv4 connections on TCP port 8888 and respond to each with a canned but plausible series of messages for now.
